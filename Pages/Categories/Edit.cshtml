@page
@model WebRentACar.Pages.Categories.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>CarCategory</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CarCategory.ID" />
            <div class="form-group">
                <label asp-for="CarCategory.CarID" class="control-label"></label>
                <select asp-for="CarCategory.CarID" class="form-control" asp-items="ViewBag.CarID"></select>
                <span asp-validation-for="CarCategory.CarID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CarCategory.CategoryID" class="control-label"></label>
                <select asp-for="CarCategory.CategoryID" class="form-control" asp-items="ViewBag.CategoryID"></select>
                <span asp-validation-for="CarCategory.CategoryID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
